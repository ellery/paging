<%= simple_form_for(@alert) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :tone_set %>
    <%= f.input :notes %>
    <%= f.input :address %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
